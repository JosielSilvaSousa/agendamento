import { SwaggerConfig } from './swagger.interface';
export const SWAGGER_CONFIG: SwaggerConfig = {
  title: 'Consulta Médica',
  description: 'Cadastro de consulta méedica',
  version: '1.0',
  tags: ['Template'],
};